你现在负责对 GitHub 仓库：

`wanghuinet/luckread`

进行一次：

# 第二轮最终全量、零信任、生产准入级审计

本次审计的唯一目标：

> **全面扫描当前仓库，找出所有会阻止 LuckRead 进入 Development Admission GREEN / 恢复 Payload 开发的真实阻断，并完成修复建议闭环。**

最终目标不是“看起来不错”，而是：

> **所有可由代码、契约、文档、Validator、CI 解决的问题全部修到 GREEN；所有外部基础设施问题必须明确隔离并给出可验证的解除条件。**

项目定位：

> LuckRead = 基于 Payload 的下一代自媒体 / 内容平台 / Creator / IP / MCN / APP Headless API 基础底座。

目标能力：

> Browser/H5 + Android/iOS 后续接入；文章、图文、视频、直播、Feed、搜索、评论、点赞、关注、通知、Creator、IP、MCN、Moderation 等。

工程标准：

> Google / Meta / Apple / Alibaba / ByteDance / Baidu / NetEase / Tencent 级别。

---

# 0. 当前必须重新建立真实基线

严禁直接引用第一轮审计结果。

必须重新读取当前 GitHub `main`：

1. 当前 HEAD SHA
2. 最近 commits
3. 当前工作树
4. 当前 workflows
5. 当前 contracts
6. 当前 docs
7. 当前 validators
8. 当前 Actions runs
9. 当前 Issues / PR
10. 当前 CI Evidence

**必须以当前 HEAD 为唯一审计基准。**

任何旧 SHA：

`PASS`

都不得作为当前 PASS。

---

# 1. Zero-Trust Admission Rule

以下任何状态：

* UNKNOWN
* MISSING
* UNVERIFIED
* NOT_EXECUTED
* WRONG_SHA
* STALE
* SKIPPED
* EMPTY_LOG
* NO_RUNNER
* PARTIAL
* continue-on-error
* local-only

全部不得判 PASS。

真正 PASS 必须满足：

```text
Current HEAD SHA
       ↓
GitHub Actions
       ↓
Runner Assigned
       ↓
Steps Executed
       ↓
Logs Available
       ↓
Validator Executed
       ↓
Exit Code 0
       ↓
Evidence Bound to Current SHA
```

---

# 2. Actions / Runner 必须首先审计

检查：

`.github/workflows/*.yml`

重点：

* workflow 是否能启动
* trigger
* permissions
* runs-on
* matrix
* needs
* skipped
* runner assignment
* steps
* logs
* artifacts

特别检查是否存在：

```text
runner_name=""
steps=[]
logs empty
job failure before first step
```

如果存在：

必须判：

`P0 — EXECUTION INFRASTRUCTURE BLOCKER`

并进一步判断：

* repository configuration
* GitHub Actions entitlement
* billing / quota
* organization policy
* runner availability
* workflow permission
* GitHub platform failure

**不要把没有 runner 的 failure 错误归类成 contract failure。**

同时：

> 无 Runner 不能阻止代码侧继续做静态/本地验证，但不能伪造 GitHub Evidence GREEN。

---

# 3. Contract Tree 全扫描

递归检查：

```text
contracts/**
```

必须建立完整 Contract Inventory。

至少扫描：

* common
* schemas
* enums
* state-machines
* authz
* openapi
* operation-policy
* media
* security
* audit
* notification
* creator
* content
* account
* user
* IP
* organization
* moderation

对每个 domain 建立：

```text
Requirement
→ Documentation
→ Machine-readable Contract
→ Validator
→ CI
→ Evidence
→ Admission
```

任何一个环节断裂：

必须列出来。

---

# 4. JSON Schema 深度审计

逐文件检查：

* `$schema`
* `$id`
* required
* properties
* additionalProperties
* enum
* const
* pattern
* format
* minimum
* maximum
* minItems
* maxItems
* uniqueItems
* oneOf
* anyOf
* allOf
* nullable/null
* ref

重点检查：

### Cross Reference

所有：

```text
$ref
```

必须真实解析。

检查：

* relative ref
* external ref
* broken ref
* circular ref
* stale ref
* duplicate canonical source

原则：

> One fact → One canonical source.

---

# 5. OpenAPI 全量审计

目标：

`contracts/openapi/v1/openapi.yaml`

逐 operation 检查：

* operationId
* summary
* description
* security
* request
* response
* 4xx
* 5xx
* pagination
* cursor
* permission
* audit
* If-Match
* Idempotency-Key
* state-machine

并建立：

```text
OpenAPI operationId
      ↕
Operation Policy
      ↕
Permission
      ↕
State Machine
```

必须严格 1:1。

---

# 6. Operation Policy 深度审计

检查：

`contracts/openapi/v1/operation-policy.json`

必须：

* schema 自己合法
* instance 自己合法
* version 正确
* operationId 唯一
* OpenAPI ↔ Policy 一致
* auth.mode 合法
* permission 合法
* auditRequired 正确
* optimisticLockRequired 正确
* idempotencyRequired 正确
* stateMachine 正确

特别检查：

### Public

public operation：

必须：

* public
* 无 permission
* 与 OpenAPI `security: []` 一致

### Permission

permission operation：

必须：

* 有 permission
* permission 在 catalog
* audit 语义一致

### State Machine

必须：

* stateMachine 明确
* transition permission 可解析
* optimistic locking 完整

---

# 7. 必须审计 operationBlock()

重点审查：

`scripts/contract-semantic-ci.mjs`

如果存在：

```js
operationBlock()
```

必须回答：

> 是否存在 false positive / false negative？

测试：

* operationId substring
* duplicated names
* reordered methods
* description 中出现 operationId
* `$ref` 中出现 operationId
* nested structures
* YAML formatting variations

如果 regex/字符串解析可能漏掉真实错误：

### 不能直接判 GREEN。

提出最小安全修复。

优先：

> proper YAML parser

或者构建更可靠的 structural representation。

---

# 8. Semantic Validator 审计

检查：

`scripts/contract-semantic-ci.mjs`

必须确认能够真正阻止：

### State

* enum mismatch
* terminal outgoing transition
* recoverable + terminal conflict
* unknown permission

### Error

* ErrorCode canonical reference
* requestId
* traceId

### Pagination

* data.items
* nextCursor
* hasMore
* requestId
* traceId

### Operation Policy

* version
* auth
* permission
* state machine
* operationId 1:1

### Concurrency

* If-Match
* 412
* 428

### Idempotency

* Idempotency-Key

如果 validator 漏检：

直接列为：

`P0/P1 validator false-negative`

---

# 9. Structural Validator 审计

检查：

`scripts/contract-ci.mjs`

尤其检查是否存在：

> “文件存在 = PASS”

例如 OpenAPI 不应只检查：

* path count
* top-level key

而忽略：

* YAML syntax
* ref validity
* security
* responses
* schema consistency

要求：

> Structural validator 必须真正对它负责的领域提供有效约束。

---

# 10. Error Contract

统一检查：

```json
{
  "error": {
    "code": "...",
    "message": "...",
    "details": {}
  },
  "requestId": "...",
  "traceId": "..."
}
```

禁止：

* request_id
* trace_id
* error_code
* next_cursor
* has_more

检查：

`docs`
`schemas`
`OpenAPI`
`validators`

全部一致。

---

# 11. Pagination / Cursor

必须统一：

```text
data.items
data.nextCursor
data.hasMore
requestId
traceId
```

检查：

* empty list
* cursor expiration
* cursor integrity
* stable order
* duplicates
* missing data
* offset leakage

搜全仓库：

```text
offset
page=
request_id
trace_id
next_cursor
has_more
```

任何 canonical contract 外残留：

必须定位。

---

# 12. If-Match / Optimistic Lock

逐 operation 检查所有 versioned mutation。

重点：

* updateMe
* updateContent
* deleteContent
* transitionAccountState
* transitionContentState

检查：

```text
If-Match
412
428
ETag
version
```

Policy：

```text
optimisticLockRequired=true
```

必须与 OpenAPI 完整对应。

---

# 13. Idempotency 深度审计

不要默认所有 mutation 都需要 Idempotency-Key。

必须依据 canonical contract。

重点检查：

* registration
* createContent
* publish
* createMedia
* createComment
* interaction
* follow
* notification batch

尤其：

```text
transitionContentState
```

必须回答：

> 是否所有 state transition 都要求幂等？

还是：

> 只有 `to=PUBLISHED` 强制 Idempotency-Key？

如果当前 boolean 模型无法表达条件：

必须指出这是 contract modeling gap。

不得通过简单把：

```text
idempotencyRequired=false
```

来制造 GREEN。

---

# 14. State Machine 深度审计

检查：

```text
account
content
media
comment
organization
organization-member
IP
moderation-case
```

区分：

### 真正必须有 State Machine 的核心实体

与：

### 当前阶段可以 enum-only 的实体。

至少必须审计：

> Media 是否应该拥有正式 State Machine。

检查：

* valid transition
* invalid transition
* terminal
* recoverable
* actor
* permission
* system transition
* concurrency
* audit

---

# 15. Account / User 生命周期

完整检查：

```text
UNREGISTERED
PENDING_VERIFICATION
ACTIVE
RESTRICTED
FROZEN
SUSPENDED
BANNED
DELETION_REQUESTED
DELETION_PENDING
DELETED
RESTORED
REACTIVATED
```

必须验证：

* terminal
* tombstone
* cooling off
* reactivation
* reinstate
* restore
* token invalidation
* deindex
* permission

特别检查：

> DELETED 是否 terminal？

> DELETED 是否 recoverable？

二者不能互相矛盾。

---

# 16. AuthZ / RBAC / ABAC

检查：

```text
L0-L8
Role
Permission
Scope
Entitlement
Subscription
Organization
Account State
MFA
Principal
Impersonation
```

检查：

* 每个 operation permission 是否存在
* 每个 transition permission 是否存在
* 每个 permission 的 layer 是否合理
* sensitive permission 是否 audit
* MFA 是否完整
* scope 是否正确

---

# 17. IP Founder / Principal

严格检查 ADR-I001。

如果：

> founder 不是 role，而是 principal identity

那么：

* docs
* schema
* authz
* OpenAPI
* IP model

必须全部一致。

严禁：

> 一部分用 founder role，一部分用 principal。

---

# 18. Security 深度审计

必须重点检查：

* IDOR
* privilege escalation
* auth bypass
* impersonation bypass
* local API bypass
* cache authorization bypass
* account enumeration
* replay
* CSRF
* CORS
* rate limit
* SSRF
* upload abuse
* MIME spoofing
* signed URL
* object access
* R2 key traversal

尤其必须确认：

> Cache 永远不是 authorization boundary。

---

# 19. Media / R2 / D1

检查：

```text
D1
metadata
state
ownership
R2
binary
checksum
upload
processing
moderation
quarantine
```

必须检查：

* orphan objects
* duplicate objects
* failed upload
* retry
* DLQ
* quarantine
* deletion
* retention
* signed URL
* resumable upload
* multipart
* idempotency

---

# 20. Observability

检查：

* requestId
* traceId
* actor
* actorType
* layer
* sessionId
* impersonatingActorId
* auditEvent
* occurredAt

必须回答：

> auditEvent 能不能关联 requestId / traceId？

如果不能：

列为契约缺口。

---

# 21. 文档全量一致性扫描

扫描：

```text
README.md
AGENTS.md
docs/**
contracts/**
scripts/**
.github/**
```

检测：

### Naming Drift

* request_id
* trace_id
* next_cursor
* has_more

### State Drift

旧 state 是否还存在？

### Permission Drift

旧 permission 是否存在？

### Role Drift

旧 role 是否还存在？

### Endpoint Drift

文档 endpoint 是否已废弃？

---

# 22. Product Capability Coverage

不要只做技术审计。

建立：

```text
Requirement
Doc
Contract
Schema
OpenAPI
Validator
CI
Evidence
```

至少覆盖：

### User

register
login
logout
profile
account state
verification
security

### Content

create
draft
edit
review
schedule
publish
unpublish
archive
restore
delete

### Feed

following
recommendation
hot
creator
IP
topic
cursor

### Social

comments
likes
follows

### Media

image
video
audio
live
upload
processing

### Creator

profile
dashboard
analytics
publishing

### IP / MCN

IP
principal
organization
member
delegation
scope

### Notifications

list
read
unread
delete
batch

### Moderation

case
review
decision
audit

发现：

> Requirement 有，但 Contract 没有

必须列出来。

---

# 23. 冗余 / 死代码 / 死契约

检查：

* duplicated schemas
* duplicated enums
* duplicate permissions
* dead permissions
* dead endpoints
* obsolete workflow
* old Payload implementation
* old deployment files
* obsolete docs
* old API version

必须分类：

```text
Conflict
Dead
Redundant
Intentional
```

不要把“历史文件”自动算作 blocker。

---

# 24. Evidence DAG

检查是否存在：

```text
Requirement
   ↓
Contract
   ↓
Validator
   ↓
CI
   ↓
Execution
   ↓
Evidence
   ↓
Admission
```

任何断边：

直接标：

`EVIDENCE GAP`

尤其：

> validator 文件存在 ≠ validator 被 GitHub Actions 执行。

---

# 25. CI Dependency DAG

检查：

```text
contract matrix
      ↓
semantic
      ↓
full
      ↓
openapi lint
      ↓
admission
```

检查：

* needs
* skipped
* failure propagation
* matrix
* continue-on-error
* exit code

任何：

> required gate skipped

必须判 RED。

---

# 26. 当前本地修复与 GitHub Evidence 必须严格分开

可以：

> 本地验证 GREEN

但是必须写：

`LOCAL VERIFIED`

不能写：

`ADMISSION GREEN`

只有：

> current HEAD + GitHub Actions + real runner + real logs + successful required jobs

才能：

`EVIDENCE GREEN`

---

# 27. 发现问题后不要只做报告

对于确定性、无业务歧义的阻断：

必须给出：

```text
Exact File
Exact Field
Current Value
Expected Value
Why Blocked
Minimal Fix
Validation Required
```

不要因为“可能以后有用”添加大量新架构。

---

# 28. 最重要：不要为了 GREEN 降标准

禁止以下行为：

### 禁止 1

把 validator 删除。

### 禁止 2

把规则改成 warning。

### 禁止 3

把 continue-on-error 打开。

### 禁止 4

把 semantic gate 从 CI 移除。

### 禁止 5

把 required=false 改掉只是为了绕过检查。

### 禁止 6

关闭 Redocly error。

### 禁止 7

删除问题对应的 contract。

### 禁止 8

把 unknown 当 PASS。

### 禁止 9

把 skipped 当 PASS。

### 禁止 10

把旧 SHA GREEN 当当前 GREEN。

---

# 29. 最终输出必须严格采用以下结构

## A. EXECUTIVE VERDICT

只能：

```text
GREEN
YELLOW
RED
BLOCKED
```

并说明：

> 当前是否允许进入 Payload Development。

---

## B. P0 BLOCKERS

表：

| ID | Blocker | Evidence | File | Exact Issue | Minimal Fix | Re-Validation |
| -- | ------- | -------- | ---- | ----------- | ----------- | ------------- |

---

## C. P1 BLOCKERS

同格式。

---

## D. P2 / NON-BLOCKING

只放：

* 优化
* 文档质量
* 开发体验
* 次要规范

不要混入 P0/P1。

---

## E. HIDDEN BLOCKERS

专门列：

> 表面通过但实际上没有被机器证明的内容。

---

## F. CONTRACT CONSISTENCY MATRIX

必须：

| Domain | Requirement | Doc | Schema | OpenAPI | Validator | CI | Evidence | Admission |
| ------ | ----------- | --- | ------ | ------- | --------- | -- | -------- | --------- |

每个 domain 必须填：

`GREEN / RED / MISSING / UNKNOWN / STALE / NOT_EXECUTED`

严禁空白。

---

## G. MINIMAL REMEDIATION PLAN

按：

```text
P0
↓
重新验证
↓
P1
↓
重新验证
↓
P2
```

不要一次塞入无关的新功能。

---

## H. FINAL ADMISSION GATE

必须逐项回答：

1. Structural Contract
2. Semantic Contract
3. OpenAPI
4. Operation Policy
5. AuthZ
6. State Machine
7. Security
8. Error/Pagination
9. Media/D1/R2
10. Validator
11. CI
12. Evidence
13. Documentation parity
14. Product capability coverage
15. No P0
16. No P1
17. Payload implementation allowed

最终只能输出：

```text
ADMISSION GREEN
```

或：

```text
ADMISSION BLOCKED
```

---

# 30. 最终目标

你不是来评价项目“好不好”。

你是来完成：

> **Admission Blocker Elimination。**

目标：

### 第一阶段

找尽所有真实 P0/P1。

### 第二阶段

区分：

`代码问题`
`契约问题`
`文档问题`
`Validator问题`
`CI问题`
`Evidence问题`
`外部GitHub执行环境问题`

### 第三阶段

对可以自动修复、没有业务歧义的项目内问题：

给出最小、确定性修复。

### 第四阶段

重新扫描。

直到：

> 不再存在已知 P0/P1。

### 第五阶段

最后进行一次：

> **Pre-Development Admission Audit**

确认：

```text
Contract GREEN
Semantic GREEN
OpenAPI GREEN
AuthZ GREEN
State Machine GREEN
Security GREEN
Validator GREEN
CI GREEN
Evidence GREEN
Documentation GREEN
Admission GREEN
```

只有此时：

> **才允许恢复 / 拉取 / 固定批准版本的 Payload 实现，进入 Development Phase。**

本次审计严禁提前开始 Payload 功能开发。

直接开始当前 `main` 的全量审计，不要先提问，不要复述任务。
