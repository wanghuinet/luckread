# LuckRead｜333333.md

## 第三层：Mapping 0 下游闭环——DeepSeek 执行规范

### 1. 任务目标

本阶段只处理 Mapping 0 的**下游闭环**，不重新建立 Mapping 0，不重新设计 Contract，不扩展架构。

目标链：

`Feature → Mapping → Contract → API/DTO → Entity/Field → Persistence → Runtime → Security → Test → Evidence → Five-Way → R4 → Evidence Registry`

最终只有在证据真实存在、来源可追溯、当前事实一致时，才允许把相应状态推进到 GREEN。

---

## 2. 唯一事实来源

每次执行必须从 GitHub `main` 开始，并记录：

- `CURRENT_MAIN_SHA`
- 当前 Canonical Mapping 状态
- 当前 Closure Ledger
- 当前 Change-Control Queue
- 当前 Five-Way Alignment
- 当前 R4
- 当前 Evidence Registry
- 当前最新 Closure Cursor

不得以本地未提交文件、旧聊天记录、旧截图或旧报告代替 GitHub `main`。

---

## 3. 当前已知基线

当前 Mapping 结构层已经完成：

- Canonical Feature：449
- Canonical Mapping：449
- Feature↔Mapping：1:1
- Structural Missing：0
- Structural Orphan：0
- Structural Gate：GREEN

当前技术闭环仍未完成。最新 Closure Ledger 记录：

- PARTIAL：17
- UNRESOLVED：431
- MISSING：1
- MISSING：AUTH-007
- Five-Way Alignment：仍有下游 blocker
- R4：449 blocked
- Evidence Registry：NOT_GREEN

这些数字必须以最新 `main` 的最新证据为准；历史快照不得覆盖当前事实。

---

## 4. 本阶段的核心规则：不循环

建立 `CURRENT_CURSOR`。

每次继续任务时：

1. 读取 Closure Ledger；
2. 读取最新 Change-Control Queue；
3. 找到 `NEXT_ITEM_ID`；
4. 读取该项目已有 Evidence；
5. 判断输入是否发生变化；
6. 没有变化则继承已有有效证据；
7. 只有发生输入变化才重新验证。

以下输入变化才允许触发重新验证：

- Contract changed
- Blueprint changed
- Mapping changed
- Entity binding changed
- Persistence schema changed
- Runtime implementation changed
- Worker binding changed
- Migration changed
- Security contract changed
- Test changed
- Evidence scope changed
- tested commit 不再覆盖当前事实

纯 Evidence / Ledger / 文档提交，如果不改变上述输入，不得导致整套验证重跑。

---

## 5. 什么叫“下游闭环”

每条 Mapping 必须分别检查：

### 5.1 Contract

确认：

- Contract ID
- Contract 是否 canonical
- Contract 是否 GREEN/PASS
- operation / DTO / state / permission 是否已有权威定义

禁止从代码反推 Contract。

缺少 Contract 时：

`GAP → Change Control → Authority Decision`

不得自行补设计。

### 5.2 API

确认：

- canonical operationId
- route
- API Inventory
- OpenAPI admission
- path representation

`DISCOVERY`、`DRAFT`、普通实现代码不能自动升级为 canonical API。

### 5.3 DTO

确认：

- DTO 是否已经具有 canonical authority
- 字段是否有公开 wire-schema 定义
- request/response 是否明确

禁止把 Entity 字段自动当成 DTO 字段。

### 5.4 Entity / Field

确认：

- Entity ID
- Entity authority
- Field authority
- Feature binding

Entity Catalog 中存在某 Entity，不代表该 Feature 已经绑定。

禁止因为“看起来应该属于这个 Entity”而建立绑定。

### 5.5 Persistence

确认：

- 数据库/D1
- table
- collection
- migration
- schema
- index/FK
- migration history

只有真实可验证证据才能标记 Persistence VERIFIED。

禁止：

- 猜 table
- 猜 column
- 手写 DDL 绕过 Contract
- 用 Entity 名称代替数据库证据
- 用 generated migration 代替实际 schema evidence

### 5.6 Runtime

确认：

- Worker
- route
- handler
- implementation
- 实际运行结果

源码存在 ≠ Runtime Verified。

### 5.7 Security

确认：

- authentication
- authorization
- scope
- role
- entitlement
- subscription
- organization
- account state
- security E2E

已有 PASS_VERIFIED 的 Security Contract 不得重新设计。

### 5.8 Test

区分：

- TEST_EXISTS
- TEST_PASS
- INTEGRATION_PASS
- E2E_PASS

测试文件存在不能自动证明运行成功。

### 5.9 Evidence

Evidence 必须能回答：

> 谁、在什么 commit、对什么对象、执行了什么检查、结果是什么？

必须检查：

- evidenceId
- source
- sourceRef
- commitSha
- timestamp
- producer
- result
- status
- validity

---

## 6. Evidence 继承

如果测试输入完全没有变化，可以：

`PASS_INHERITED`

必须记录继承依据：

- 原 Evidence
- 原 tested SHA
- 当前 SHA
- 哪些输入保持不变

不得因为 HEAD 前进一个文档 commit 就重新执行所有测试。

---

## 7. 当前关键阻塞

### AUTH-002

`W01-MIGRATION-BASELINE-AUTHORITY-001` 当前仍为：

`BLOCKED_EXTERNAL`

远程 D1 baseline evidence 尚未完成。

不得：

- 模拟 Cloudflare 执行
- 伪造 remote evidence
- 手工修改 schema
- 添加未经授权 migration
- 把 local Miniflare evidence 当 remote D1 evidence

已有 W01 migration safety guard 可以验证，但它不能代替远程执行证据。

### AUTH-003

AUTH-003 当前仍：

`BLOCKED_NOT_GREEN`

Canonical operation 已固定：

- authCredentialList
- authCredentialAdd
- authCredentialReplace
- authCredentialRemove

但 exact public wire schema 仍未授权。

9 个未决项不得自行决定：

1. Add request fields/requiredness
2. Replace request fields/requiredness
3. Replace kind-change semantics
4. Public response projection
5. Public kind representation
6. List envelope/cursor/order
7. credentialId namespace/schema
8. success/error HTTP semantics
9. unknown-property behavior/examples

不得从 Entity、Payload、数据库、generated types 或 implementation 反推。

### AUTH-007

AUTH-007 当前为：

`MISSING`

已有审计已经证明：

`NO_CANONICAL_MFA_CONTRACT_FOUND`

因此：

- 不创建 MFA API
- 不创建 DTO
- 不创建 Entity
- 不创建 Mapping edge
- 不创建 runtime

必须等待新的 Authority Decision。

---

## 8. W01 特别保护

当前 W01 baseline migration 只允许使用已经批准的 migration/index 范围。

不得：

- 修改已有 baseline migration
- 将 generated full-schema migration 作为第二个 migration
- 手写 ALTER TABLE
- 创建平行 session/sessions 表
- 引入 auth_session_state 作为未经批准的新 persistence
- 把 Payload 项目与 D1-Fabric 混合

W01 Payload 必须继续保持 Payload 独立项目边界。

---

## 9. 批量处理策略

不得一次性修改 449 条。

优先级：

### Batch 1
已有完整 Entity + Persistence + Runtime + Test + Evidence 的记录。

### Batch 2
已有 Entity/Persistence，仅缺 Runtime/Evidence。

### Batch 3
已有 Entity，仅缺 Persistence Evidence。

### Batch 4
已有 API/Contract，但缺 Entity binding。

### Batch 5
已有安全/测试/运行证据，仅缺 Mapping linkage。

### Batch 6
真正 Authority Gap。

Authority Gap 不得自行解决，只形成：

`GAP → Change Control → Authority Required`

每批建议 10～25 条。

---

## 10. 当前 43 条显式下游记录的处理规则

已有 triage 将显式 AUTH/USER/AUTHZ/ORG 记录分类为：

- `DOWNSTREAM_DTO_FIELD_PERSISTENCE_RUNTIME_EVIDENCE_RECONCILIATION_REQUIRED`
- `CANONICAL_API_ADMISSION_RECONCILIATION_REQUIRED`
- `FEATURE_BINDING_RECONCILIATION_ONLY`

这些分类不能被普通文字或 Entity Catalog 自动提升。

尤其：

- API Inventory 存在 ≠ canonical API admission
- Entity 存在 ≠ Feature Entity binding
- Payload collection 存在 ≠ Feature persistence
- Evidence reference 存在 ≠ Evidence proves completion

---

## 11. B04-B20 规则

已有审计对 B04-B20 的主要 mapping batch 做过 repository-only edge sweep。

如果当前 batch 仍然没有明确：

- API edge
- Entity edge
- Payload edge
- Code evidence edge

就不得从描述文字中补边。

必须等待真实权威来源。

不要重复进行同一 sweep。

---

## 12. 状态转换

允许：

- UNRESOLVED → PARTIAL
- UNRESOLVED → GREEN
- PARTIAL → GREEN

但必须存在真实依据。

不允许：

- UNRESOLVED → GREEN，仅因为代码“看起来完成”
- PARTIAL → GREEN，仅因为 Entity 存在
- MISSING → GREEN，仅因为 Blueprint 有描述

---

## 13. Five-Way Alignment

Five-Way Alignment 是下游闭环结果，不是可以通过修改报告数字来解决的 Gate。

不得：

- 删除 blocker
- 修改 Gate 条件
- 放宽判定
- 把未验证关系写成 verified

只有真实 Contract/API/DTO/Entity/Field/Persistence/Runtime/Evidence 对齐后，才会自然减少 blocker。

---

## 14. R4

R4 当前重点是：

- Entity binding
- Persistence verification
- Evidence readiness

不得用：

- Entity 名称
- 文档说明
- 代码路径

替代真正的 Evidence。

---

## 15. Evidence Registry

Evidence Registry 只有在 Feature-level evidence 真实满足条件时才允许推进。

历史 Evidence 如果已经过期：

- 保留历史 provenance
- 不改写历史结果
- 不伪造新的 validUntil
- 如果输入未变化且规则允许，建立合法继承
- 如果输入变化，则重新产生对应证据

---

## 16. Git 提交规则

每批修改：

- 最小范围
- 单一目的
- 不混入重构
- 不修改 Contract，除非已有 Change Control 明确授权
- 不修改 Blueprint，除非已有 Change Control 明确授权
- 不修改 Gate
- 不修改架构

建议 commit：

`mapping-0: close downstream batch N`

如果本批没有任何合法状态变化：

> 不制造空洞“修复”提交。

可以只更新 continuation cursor / audit artifact。

---

## 17. 每批验证

只运行受影响的最小必要检查：

1. Mapping reconciliation
2. Five-Way Alignment
3. R4
4. Evidence Registry impact
5. 相关 Contract CI checks

如果输入未变化：

> 继承历史结果，不重复运行。

---

## 18. 每批 Closure Record

必须记录：

```
Batch ID:
Source main SHA:
Input evidence:
Mapping IDs:

Before:
After:

GREEN:
PARTIAL:
UNRESOLVED:
MISSING:

API:
DTO:
Entity:
Field:
Persistence:
Runtime:
Security:
Test:
Evidence:

Inherited Evidence:
New Evidence:

Five-Way delta:
R4 delta:
Evidence Registry delta:

Authority blockers:
External blockers:

Next Cursor:
```

---

## 19. 最重要的反循环规则

遇到已经验证过的项目：

> PASS_INHERITED

遇到同一个 authority blocker：

> LINK_EXISTING_BLOCKER

遇到同一个 evidence：

> REUSE_EXISTING_EVIDENCE

遇到没有新事实：

> NO_CHANGE

禁止：

> 为了“继续”而重新搜索同一问题。

---

## 20. 最终验收标准

第三层不是“改了很多文件”。

第三层完成的定义：

1. 所有可以根据现有权威事实闭环的 Mapping 已闭环；
2. 所有不能闭环的 Mapping 都有明确 blocker；
3. blocker 能追溯到 Contract / Authority / Runtime / Persistence / Evidence；
4. 没有伪造 Entity；
5. 没有伪造 Persistence；
6. 没有伪造 Runtime；
7. 没有伪造 Evidence；
8. 没有弱化 Gate；
9. 没有修改既有合法 Contract；
10. 没有架构漂移；
11. Five-Way Alignment blocker 实际下降；
12. R4 ready 数量实际增加；
13. Evidence Registry 实际获得有效 Feature-level evidence；
14. 当前 Cursor 明确指向下一项，而不是回到第一项。

---

## 21. 当前执行指令

现在立即执行：

```
READ latest GitHub main
↓
READ Closure Ledger
↓
READ Change-Control Queue
↓
READ current downstream evidence
↓
BUILD CURRENT_CURSOR
↓
SELECT next unclosed downstream batch
↓
REUSE valid evidence
↓
PROCESS ONLY legitimate closure
↓
VERIFY affected gates
↓
COMMIT minimal change
↓
UPDATE cursor
↓
CONTINUE
```

禁止重新扫描已经 PASS 的 Structural Gate。

禁止重新处理已经 CLOSED 的 Change Control。

禁止为了获得 GREEN 而推断任何缺失关系。

如果本批没有合法可闭环项，必须明确输出：

`NO_SAFE_CLOSURE_AVAILABLE`

并列出真正阻塞原因。

---

# 22. 当前项目边界

本阶段仍然属于 Mapping 0。

因此：

- 不进入业务功能开发
- 不新增 Worker
- 不新增 D1
- 不改变 12 Worker / 4 D1 既定架构
- 不将 D1-Fabric 放入 Payload
- 不提前开发 AUTH-003
- 不提前开发 AUTH-007
- 不把 Mapping 结构 GREEN 等同于产品开发 GREEN

只有 Mapping 0 下游证据闭环后，才进入后续开发准入。

---

## 23. DeepSeek 输出要求

每次完成后只输出：

### 完成
- Batch
- Commit
- 实际改变的 Mapping
- 实际新增 Evidence

### Gate
- Five-Way
- R4
- Evidence Registry

### 未完成
- Authority blockers
- External blockers

### 下一步
- NEXT_ITEM_ID
- NEXT_ITEM_STATE

不得声称“全部完成”，除非 GitHub 当前 main 的真实 Evidence 和 Gate 全部满足完成条件。
