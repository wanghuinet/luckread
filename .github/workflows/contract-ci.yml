name: Contract CI

on:
  push:
    branches: [main, 'contract/**']
    paths:
      - 'contracts/**'
      - 'scripts/contract-ci.mjs'
      - '.github/workflows/contract-ci.yml'
  pull_request:
    paths:
      - 'contracts/**'
      - 'scripts/contract-ci.mjs'
      - '.github/workflows/contract-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Contract CI is the CODE DEVELOPMENT ADMISSION gate.
  # Contract -> Schema -> OpenAPI -> Migration -> DTO -> Authorization ->
  # State Machine -> Test -> Contract CI -> Code Development Admission.
  contract:
    name: Contract Admission Gate
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        domain: [common, enums, state-machines, authz, openapi]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Runner diagnostics
        shell: bash
        run: |
          set -euxo pipefail
          printf 'PWD=%s\n' "$PWD"
          node --version
          npm --version
          test -d contracts
          test -f scripts/contract-ci.mjs
      - name: Validate contract domain (${{ matrix.domain }})
        shell: bash
        run: node scripts/contract-ci.mjs ${{ matrix.domain }}

  contract-full:
    name: Contract Admission Gate (full)
    runs-on: ubuntu-latest
    needs: contract
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Validate all contract domains
        shell: bash
        run: node scripts/contract-ci.mjs

  openapi-lint:
    name: OpenAPI Lint
    runs-on: ubuntu-latest
    needs: contract-full
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Lint OpenAPI
        shell: bash
        run: npx --yes @redocly/cli@1.34.2 lint contracts/openapi/v1/openapi.yaml
