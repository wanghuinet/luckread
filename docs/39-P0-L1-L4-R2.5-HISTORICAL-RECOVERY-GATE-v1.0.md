# Luckread P0 — L1-L4 Historical Recovery Gate v1.0

**Status:** IMPLEMENTED / FAIL-CLOSED / AWAITING CI EVIDENCE

## 1. Objective

Close the R2.5 recovery step before any historical L1-L4 hierarchy is admitted into the active Capability Contract Graph.

## 2. Authoritative boundary

- `docs/00-LUCKREAD-ULTIMATE-FEATURE-BLUEPRINT-v2.0.md` remains the active product Feature source of truth.
- Historical 1.0 matrices are recovery evidence only.
- No historical hierarchy is promoted automatically.
- Feature IDs are never assigned by name similarity, API similarity, Payload collection similarity, or database-table similarity.

## 3. Recovered source

`docs/archive/1.0-reuse/RECOVERED-36-FOURTH-LEVEL-CAPABILITY-MASTER-MATRIX-v1.0.md`

The deterministic parser is:

`scripts/recover-l1-l4-hierarchy.mjs`

The admission gate is:

`scripts/l1-l4-recovery-gate.mjs`

## 4. Gate behavior

The gate requires deterministic recovery of explicit L1, L2, L3 and L4 nodes and rejects unresolved L4 candidates. It never invents an L3 parent for an L4 bullet that lacks an explicit L3 heading.

The recovered result remains `RECOVERED_NON_AUTHORITATIVE`; a successful R2.5 gate only permits the next reconciliation stage. It does not make historical hierarchy authoritative.

## 5. CI integration

`contract-ci.yml` now executes the R2.5 recovery gate before Blueprint-to-Registry and L1-L4 admission checks.

## 6. Next blocking stage

After R2.5 passes, perform the deterministic Feature Crosswalk:

`Blueprint Feature ID -> recovered L1/L2/L3/L4`

Only explicit evidence may produce a `MATCH`. Unmatched or conflicting mappings remain blocked.

## 7. Release rule

No Capability Graph GREEN promotion is allowed until the Feature Crosswalk, owner, API, domain/entity, persistence, permission, state, event and evidence mappings are all resolved and CI proves them.
