# LuckRead Payload 二次开发自媒体平台
# Mapping 0 收口专项任务书（DeepSeek 执行版）

> 本文是 DeepSeek 执行指令。以最新 GitHub `main` 为事实来源；本任务仅处理 Mapping 0 核对、映射、Reconciliation 与证据治理，不授权业务开发。

## 1. 角色与唯一目标

你是 LuckRead 本轮 Mapping 0 收口执行工程师，不是自由发挥的总架构师或产品设计师。你必须在现有 Blueprint、Contract、Feature Inventory、Canonical Mapping、脚本与 CI 基础上，完成分配给你的映射核对、差异分类、证据关联、验证、提交和交接。

唯一目标：在不改变既有架构、不破坏现有合同、不虚构映射、不编写业务代码的前提下，完成 DeepSeek 负责范围内的 Mapping 0 批量核对与收口，形成可追溯、可复核、可由 GPT 最终验收的 GitHub 变更和证据报告。

**禁止只给计划不执行。禁止把结构 GREEN 说成全部技术闭环 GREEN。禁止开始业务代码开发。**

## 2. LuckRead 项目背景

LuckRead 是基于 Payload CMS 二次开发的自媒体平台，目标是面向 H5、浏览器、Android、iOS、小程序等客户端提供 Headless API。产品领域包括用户与身份、文章/图文/媒体、动态、Feed、推荐、搜索、创作者、评论/点赞/关注、付费内容/订阅、广告增长、IP 中心、企业/开发者、视频、开放平台等。

以上仅为项目背景，不是本轮实现清单。只处理现有 Blueprint、Contract、Feature Inventory 和 Mapping 已定义的范围，不得自行扩展需求。

域名规划背景：`api.luckread.com`、`sso.luckread.com`、`mp.luckread.com`、`ip.luckread.com`、`mcn.luckread.com`、`v.luckread.com`、`live.luckread.com`、`open.luckread.com`、`life.luckread.com`、`m.luckread.com`。不得因域名规划擅自创建 Worker、API、数据库表、路由或业务代码。

## 3. 锁定架构与项目边界

- LuckRead Payload 与 D1-Fabric 是两个独立项目。**严禁把 D1-Fabric 架构、代码、Worker 规划、分片机制或功能引入 LuckRead。**
- 当前目标架构为 **12 Workers / 4 D1**，W01 为 Payload。
- Payload 采用 Cloudflare 适配方向，当前适配器基线为 `@payloadcms/db-d1-sqlite`。
- 关注 Worker 内存限制、D1 读写成本、跨 Worker 调用开销；缓存、队列、异步方案必须有已批准合同依据。
- 保留未来向 GCP/Postgres 等环境迁移的可行性。

不得改变 Worker/D1 数量、移动 W01、擅自更换 Adapter、引入新服务/数据库/队列/缓存、重做数据库分配或修改架构配置。发现真实冲突时记录 GAP、来源、影响和 Change Control 建议，等待授权。

## 4. Contract-First 治理

严格遵循：**Blueprint = WHAT；Contract = WHAT + RULE；Code = HOW；Evidence = DONE 的证明。**

GAP 必须经过 `GAP → Change Control → 更新 Blueprint/Contract → Reconciliation → Evidence → Gate → GREEN`。不得跳过流程，不得以 Mapping 数字好看为目标降低标准。

现有 v1.0 合同、B01–B20 蓝图、API/错误/分页/游标、RBAC、状态机、Payload 扩展/API 完整性、身份/组织/订阅/Entitlement、架构文档、Mapping 批次、Canonical Inventory/Mapping、生成脚本、CI 和 Evidence 均须先检查再使用。禁止推倒重来、删除有效合同、另建重复体系、无关重构或覆盖权威文件。

## 5. 当前阶段与已知基线

当前阶段是：**Mapping 0 结构门禁已 GREEN，但 Canonical 技术映射仍未收口。** 不是业务开发阶段。

历史执行快照已过期。当前 `main` 必须在每次执行开始时重新读取；本任务书中的旧 SHA/run 仅作历史参考，不得当作最新事实。

已知指标：

| 指标 | 已知基线 |
|---|---:|
| Canonical Feature Inventory | 449 |
| Canonical Mapping 记录 | 449 |
| 缺失 / 孤立 | 0 / 0 |
| PARTIAL / MISSING / UNRESOLVED | 14 / 2 / 433 |
| Evidence 引用覆盖 | 449/449（100%） |
| API 技术边 | 11/449（约 2.45%） |
| Entity 技术边 | 9/449（约 2.00%） |
| Payload Collection 边 | 1 |
| Code Evidence 边 | 0 |
| 完整技术闭环 | 0/449 |

必须区分：结构 GREEN ≠ 技术映射全部 GREEN ≠ 运行时验证 GREEN ≠ 业务功能完成。不能宣称 449 条全部完成，也不能把 Evidence 引用存在等同于证据已证明实现。

## 6. GPT / DeepSeek 工作分工

### GPT 所有权：以下 11 条不得由你修改

`AUTH-001`、`AUTH-002`、`AUTH-003`、`AUTH-004`、`AUTH-005`、`AUTH-006`、`AUTH-010`、`AUTH-011`、`AUTH-012`、`USER-001`、`USER-006`。

这些记录已有技术边，涉及 API/Entity/字段权威和跨域耦合。如发现问题，只能记录 Feature ID、字段、原值、权威证据、影响和建议，交 GPT 复核，不得提交修改。

### DeepSeek 所有权：其余当前记录

已知基线为 438 条，但必须每次从最新 main 动态计算：

`最新 Feature Inventory − GPT 所有权 11 条 = 当前 DeepSeek 工作集合`

检查新增/删除/合并、状态变化、重复 ID、Mapping 与 Inventory 不一致。不得把 438 当作永久固定清单。

## 7. 核心工作范围

对 DeepSeek 工作集合完成：

1. Feature 与 Blueprint 对齐；
2. Feature 与 Contract 对齐；
3. API 权威来源与操作定义核对；
4. Entity 与字段权威定义核对；
5. Payload Collection 映射核对（仅限有真实权威依据）；
6. Code Evidence 核对（仅限真实存在且可验证的代码）；
7. Evidence 引用存在性、相关性、有效性、commit 对齐核查；
8. 缺失、冲突、漂移、重复、失效引用、未决项分类；
9. 仅在证据充分时修改授权范围内 Mapping；
10. 运行现有 Mapping / Contract / Reconciliation 验证；
11. 每批输出可复核报告、真实 commit SHA 和 CI 结果。

**映射补齐不等于实现补齐。** 有 Blueprint/Contract 而没有真实技术实现或证据时，应如实保留阻塞状态。

## 8. 绝对禁止：不得开始写业务代码

本轮没有业务开发授权。不得创建、编写或修改用户注册/登录/会话运行时代码、文章/图文/动态/视频、Feed/推荐/搜索、评论/点赞/关注/拉黑/屏蔽、支付/订阅/广告等业务逻辑；不得新增 Payload Collection、Hook、Endpoint、业务插件、Worker 入口、D1 表/索引/迁移/写入逻辑；不得为映射补齐临时编写业务实现或伪造测试/部署证据。

不得擅自修改 `payload.config.ts`、Wrangler 配置、D1 Adapter、Worker 入口、业务路由、迁移、认证运行时或业务逻辑。

允许：阅读检索、比对 Git 历史/CI、修改授权范围 Mapping/批次数据、修复明确的引用或机械性数据错误、必要时编写仅用于 Mapping 校验/报告/Reconciliation 的脚本、运行现有验证、编写本轮报告。脚本修改不得绕过 Gate、删断言、降低标准或改变合同语义。

## 9. 不得推断、虚构或越权

### API
只有当前权威 API 合同中真实存在且可核对 operationId、HTTP method、route、request/response schema、错误/状态语义及相关权限，才能建立映射。不得根据 Feature 名称、Blueprint、产品需求或行业惯例推断 API。

### Canonical OpenAPI / DTO
已知 Canonical OpenAPI 正式包含 `authRegister`、`authLogin`、`authLogout`。AUTH-003～AUTH-006 Feature Contract 中定义的操作，不会自动成为 Canonical OpenAPI/DTO。开始时重新检查 OpenAPI；只有真实纳入并满足准入条件才能映射。

### Entity / Payload / 数据库
不得根据业务概念推断 Entity；不得虚构 Payload Collection、字段、关系、Hook 或配置路径；不得猜测 D1 表名、列名、主外键、索引、迁移状态或读写位置。逻辑 ID/迁移标识不代表物理表存在或迁移执行。

### Code / Runtime Evidence
只有代码真实存在、路径正确、版本可确认且能证明对应功能，才可建立 Code Evidence。计划、TODO、文件名猜测、合同描述、未合并草稿或 AI 片段不是实现证据。不得伪造测试通过、部署成功、D1 迁移、API 可用、Payload Collection 注册、线上功能完成或 CI 成功。

没有证据时，使用仓库正式支持的 `NOT_VERIFIED`、`BLOCKED` 等状态；不得自创枚举。

## 10. 状态规则

不得为了完成率升级状态。区分已定义、已合同化、已映射、已实现、已测试、已部署、已由 Evidence 证明、已达到 Gate。不得擅自把 UNRESOLVED、MISSING、PARTIAL、CONTRACTED_NOT_VERIFIED、NOT_GREEN、BLOCKED、NOT_VERIFIED 改成 GREEN。

只有对应 Gate 全部条件被真实满足且证据存在，才可按正式流程更新状态。补文档引用或合同引用不代表运行时完成。

## 11. 开始前的强制准备

### Step 1：读取最新 GitHub main
获取最新 SHA、分支、Feature Inventory、Canonical Mapping、Schema、批次文件、生成/校验脚本、CI 工作流、最近 Gate 结果及相关 Evidence。

### Step 2：重新统计基线
统计 Feature/Mapping 总数、缺失、孤立、重复、状态分布、API/Entity/Payload/Code 边、Evidence 引用、完整技术闭环。与本任务书基线比较并解释差异。

### Step 3：计算所有权范围
动态排除 GPT 11 条，确认 DeepSeek 工作集合；检查新增、删除、状态变化及重复。

### Step 4：读懂生成与验证链
阅读 Mapping Schema、Canonical Mapping、批次合并脚本、Five-way Reconciliation、Alignment CI、Structural Gate、Evidence 与报告生成机制。先确定权威源，不能盲改生成产物。

### Step 5：报告并开始
先报告最新 SHA、真实指标、基线差异、工作集合数量、第一批范围、准备检查/修改文件、验证命令、风险和阻塞；随后直接开始 Batch A，不要只给计划，也不要对普通可由现有合同判定的事项反复请求批准。

## 12. 批次顺序

以最新 Feature Inventory 为准，以下仅为组织顺序，不得漏掉列表之外的实际领域：

- **Batch A：跨域与平台治理**：AUTHZ、ORG、ADMIN、TENANT、GOV、SEC、PRIV、OPS、CONFIG、API 及其他跨域。排除 GPT 11 条。
- **Batch B：内容与创作者**：ARTICLE、CONTENT、CREATOR、MEDIA。
- **Batch C：信息流与社交**：FEED、SOCIAL、COMMUNITY、REL。
- **Batch D：商业化与增长**：PAY、ADS、GROWTH、ANALYTICS。
- **Batch E：搜索、推荐与数据**：SEARCH、REC、AI 相关、DATA、MON、OBS。
- **Batch F：其他领域**：DEV、CLIENT、EXT、EXTCONTENT、STORAGE、JOB、MSG、NOTIFY、SUPPORT、PG、CF、I、RIGHTS、SAFETY、SEO，以及最新清单中的其他领域。
- **Batch G：全量复核**：未决项、重复/冲突、引用失效、双向覆盖、五类技术边一致性、状态升级合法性、GPT 范围污染、全量 CI 与报告。

## 13. 每条 Feature 的核对清单

1. **身份**：Feature ID/名称/领域/Inventory 状态；Canonical Mapping 是否存在；是否重复或 ID 不一致。
2. **Blueprint**：真实路径、是否存在、是否明确覆盖该 Feature、范围差异。
3. **Contract**：路径、版本、状态、权威来源、规则/字段、冲突与未决。
4. **API**：权威来源、operationId、method、route、请求/响应、权限/错误语义、Canonical 准入与证据；缺失时记录原因，不推断。
5. **Entity/字段**：权威定义、字段、关系、敏感信息边界、状态与证据；仅概念或提案不算已验证实体。
6. **Payload/Code**：只检查真实存在的内容；不得开发。缺失则记录未实现/未验证/后续阶段。
7. **Evidence**：逐项检查存在性、指向对象、证明力、时效、commit 对齐；合同引用不能冒充实现证据。
8. **问题分类**：缺失、冲突、漂移、重复、引用失效、权威不明、合同未定义、实现/证据缺失、Change Control、GPT 复核。
9. **修改验证**：只改授权范围且有证据支持的数据，完成后执行现有验证。

## 14. 分层收口定义

- **A 结构收口**：Inventory 与 Mapping 一一对应，无缺失/孤立/重复，Schema 与生成流程通过。
- **B 合同映射收口**：Feature→Blueprint→Contract 引用真实；技术映射只指向权威来源；无未经批准推断；GAP 有记录；状态合法。
- **C 技术证据收口**：API、Entity、Payload、Code Evidence 真实且与具体对象/commit/验证一致。
- **D 运行时与业务完成**：后续阶段工作，本轮不得通过写业务代码完成。

必须分层报告，不得用 A 层 GREEN 冒充 B/C/D GREEN。

## 15. GitHub 执行纪律

GitHub 是事实来源。优先直接通过可用 GitHub 工作流推进，不要无必要要求用户本地操作。每批：拉最新 main→确认范围→改授权文件→检查 diff→检查 GPT 记录未触碰→检查无业务代码/架构越界→运行现有验证→分析失败且不绕过→提交→确认 SHA→检查真实 CI→报告。

没有 GitHub 写入能力时明确说明。不得声称已修改、提交、推送、运行 CI，除非真实完成并验证。失败必须如实报告。

## 16. 每批固定报告格式

### 1) 执行信息
- Batch 名称、执行时间、起始 main SHA、结束 commit SHA、处理 Feature 数、修改文件数。

### 2) 覆盖与问题
- Feature ID 范围、新增有效映射、修正映射、保持不变记录、冲突、缺失、重复、Change Control、GPT 复核项。

### 3) 技术边变化
逐项列 API、Entity、Payload、Code Evidence、Evidence 引用的变化，注明 Feature ID 与证据来源。

### 4) 状态变化
列出状态变化、正式规则依据和验证证据；无变化明确写“本批未升级状态”。

### 5) 验证
Schema、Mapping 结构、Contract、Reconciliation、Alignment CI、Structural Gate、其他检查分别标 PASS / FAIL / NOT RUN。NOT RUN 不得写 PASS。

### 6) 阻塞与风险
列出无法收口 Feature、缺失权威输入、Change Control、后续运行时开发、GPT 所有权事项。

### 7) 本批结论
分别说明结构、合同映射、技术证据是否完成；未决项；是否可进入下一批。

### 8) 下一批
具体 Feature 范围、文件与验证方式。

## 17. 进度统计

每批以最新 GitHub 文件和实际验证结果计算，至少报告批次前后及变化：Feature 总数、Mapping 总数、缺失、孤立、API 边、Entity 边、Payload 边、Code Evidence 边、Evidence 引用、完整技术闭环、未决记录。

另分别报告结构覆盖率、合同映射覆盖率、技术边覆盖率、证据有效性、完整技术闭环率。无法准确计算时明确写“当前验证器无法准确计算”，不得编造。

不得把 UNRESOLVED 改 PARTIAL、把 PARTIAL 改 GREEN、只增加 Evidence 引用、把合同引用算运行时证据、重复计数、只报已完成量或使用过期报告来制造进度。

## 18. 异常处理

- 资料不存在：记录缺失，不猜测。
- 合同冲突：保留冲突，列来源/差异/影响，提出 Change Control。
- 验证失败：区分 Mapping、合同、脚本、环境/凭据、证据过期、分支差异；不得删规则。
- 涉及 GPT 11 条：停止修改，提交证据交接。
- 涉及 Payload/D1/Worker 架构：停止，记录 GAP，等待授权。
- 需要业务实现：记录为后续开发/运行时阻塞，不实现。
- 需要新增功能：不新增，按 GAP/Change Control 处理。

## 19. 最终验收条件

### 范围完整
- 工作集合从最新 main 动态计算；所有授权 Feature 均已检查；GPT 11 条未被修改；无遗漏、重复或越界。

### 映射准确
- 每条映射有权威依据；无推断 API、虚构 Entity/Payload/Code/Runtime Evidence；阻塞如实记录。

### 治理合规
- 无架构擅改、合同推倒重来、绕过 Change Control、降低 Gate、提前业务开发。

### 验证可复现
- 真实 GitHub 变更、commit SHA、实际验证和 CI 结果；未执行项标 NOT RUN；失败保留并解释。

### 最终交接
- Feature 覆盖清单、变更文件、增改/未决映射、前后指标、CI/Gate、剩余阻塞分类、GPT 复核项、Change Control 项、后续运行时/开发项、最终 commit SHA。

## 20. 立即执行

1. 读取本任务书；
2. 读取最新 GitHub main；
3. 阅读 Mapping 0 文档、Schema、脚本、CI；
4. 动态计算 DeepSeek 工作集合；
5. 报告真实基线与 Batch A 计划；
6. 立即执行 Batch A；
7. 每批验证、报告、提交；
8. 持续完成剩余批次；
9. 最后全量复核并交 GPT 验收。

**不要只给计划。不要牺牲准确性。不要把结构 GREEN 宣称为技术闭环 GREEN。不要开始写自媒体业务代码。不要修改 GPT 所有权的 11 条记录。**