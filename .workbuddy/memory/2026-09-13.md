# 2026-09-13

## LuckRead 全量设计文档审计（架构/域/契约/安全/文档治理）

### 关键发现（P0）

1. **用户认定的架构基线标识符全库命中为 0**：`W00-W08`、`D1-01/02/03`、`L0-L8`、
   `AUTHZ-CACHE-INVARIANT-001`、`主理人`。这些从未真正落库。
2. **工作区是孤儿分支**：本地 `master` 0 commits、未跟踪；文档与代码只在远程。
   - `origin/main`：225 份文档 + 应用骨架（最新 4 次提交是 P0 契约）
   - `origin/contract/v1-machine-readable`：加了 `contracts/` + CI，但**删除了 4 份 P0 文档**
     （03-API-ERROR-PAGINATION-CURSOR / 04-PERMISSION-RBAC / 05-STATE-MACHINE / 11-PAYLOAD-EXTENSION）
   - 处置：已从 `origin/main` 恢复 225 份文档到工作区。合并仍未执行。
3. **OpenAPI `paths: {}` —— 0 个端点**，契约层实际为空。
4. 代码基线是空白 Payload 模板（仅 Users / Media 两个 collection）。
5. 其他缺失：`ETag`(0)、`Soft/Hard Delete`(0)、`IDOR/XSS/SSRF/Brute Force`(0)、`Department`(0)。
6. 文档编号重复 7 组（03/04/05/11/69/70/71）；`215`/`216` 同名不同内容（OPEN-G01）；缺 `01-`。

### 架构冲突裁定

`LUCKREAD-PAYLOAD-BOUNDARY` 说「不预先固定 Worker 数量」，与 W00-W08 冲突。
→ **ADR-A001**：W00-W08 是**逻辑能力域**，v1 允许共置；拆分是部署决策不是架构决策。
同理 D1-01/02/03 = **单 D1 库内表前缀逻辑域**（D1 不支持跨库 JOIN/事务）。

### 产出

新增 11 份 P0 文档 `docs/300~311`：
- 300 架构基线（W00-W08/D1/R2/Cache 边界 + ADR-A001/A002/A003）
- 301 L0-L8 权限层级（含 DENY 优先的 9 级冲突解决顺序）
- 302 授权缓存安全（AUTHZ-CACHE-INVARIANT-001..004 + 12 项强制测试）
- 303 七类概念分离 + 12 态账户状态机
- 304 组织/企业/IP 主理人（**ADR-I001：不创建 IP 创始人角色**）
- 305 并发/ETag/条件请求/幂等
- 306 删除语义（5 级层级 + 逐实体矩阵 + ADR-D001 审计不随用户删除）
- 307 安全威胁模型（21 项）
- 308 完整性矩阵 + 反向覆盖缺口 RC-01~RC-10
- 309 文档治理 / Canonical Source / 18 类映射
- 311 最终 GREEN 报告

机器可读契约 `contracts/` 共 28 文件：common 11、enums 11、state-machines 2、authz 3、
openapi 21 端点。

`scripts/contract-ci.mjs` 升级为 5 域全量校验 + 跨契约一致性 + 不变量强制，
**已通过负向测试验证**（注入 ip_founder / fail-open / 敏感操作缓存 均能 RED）。

### 状态

- Contract CI：GREEN（本地）
- API：RED（Block/Mute、Report/Appeal、Revision 等核心 APP 能力仍无 API）
- 其余见 `docs/311`。

### 代码开发准入复核（第二轮，回答「能否进入代码阶段」）

结论：**不能**。CI GREEN 不等于可开发。三项新发现的硬阻断：

1. **应用根本构建不起来**（P0，新增）
   `src/payload.config.ts:13` → `import migrations from './db/migrations'`
   - `src/db/` **不存在**（实际是 `src/migrations/index.ts`）
   - `tsconfig.json` 的 paths 只有 `@/*` 和 `@payload-config`，无 `db` 别名 → 无兜底
   - 且 `src/migrations/index.ts` 只有 `export const migrations`，**没有 default export**
   → 路径与导入形式双重错误，`pnpm dev` / `next build` 必然失败。

2. **git 基线仍是 UNBORN**（P0，延续）
   `refs/heads/master` 指向 0 commits；225 份文档 + contracts + 应用骨架共 313 个文件
   全部只存在于 **git index（`A` 状态），从未 commit**。任何 `git reset`/`checkout` 误操作即全丢。
   注意：`origin/main` 只有 docs，**src/ 应用代码从未提交过任何分支**。

3. **无可迁移目标库**
   `wrangler.jsonc` 的 `database_id` 仍是占位符 `"DATABASE_ID"`，`database_name: "my-app"`，
   R2 binding 名 `R2` 也未实际创建 → 无法 `payload migrate`。

另确认：`collections: [Users, Media]`，领域集合 0 个；OpenAPI 21 端点中
`block`/`mute`/`report`/`appeal`/`revision` 均无端点（`revision` 仅作为 Content 字段出现）。

### 开-code 四道门禁（缺一不可）

1. commit 基线（解除 index-only 风险）
2. 修 `payload.config.ts` 导入路径 + 导入形式 → `pnpm dev` 起得来
3. 补 RC-01~RC-03 API（Block/Mute、Report/Appeal、Revision）→ API 转 GREEN
4. 落地 P0 领域集合 + 首版 migration + 真实 D1/R2 binding

可**提前**开工的部分（不依赖 D1 与 collections，契约已冻结）：
纯函数层 —— authz policy evaluator、state machine validator、cursor codec、idempotency key 编解码。

### 构建修复（第三轮）—— 已完成，`next build` 通过

用户要求「修复文档，到可以构建起来为止」。三个阻断全在 `src/payload.config.ts`（**单文件**），
文档无法绕过，故只改该文件，未新增 collection / 业务逻辑 / API（未越过 Contract-First 门禁）。

| 缺陷 | 根因 | 修复 |
|---|---|---|
| D-01 | `import migrations from './db/migrations'`：`src/db/` 不存在；且该模块无 default export | 改 `import { migrations } from './migrations'` |
| D-02 | `remoteBindings: isProduction` → 构建期连远程 D1，`database_id` 是占位符 → 10021 | 改 `isProduction && isPayloadCLI` |
| D-03 | `isCLI` 靠 `process.argv` 判断，但 Next 的 page-data 收集 **worker 是 fork 出来的、argv 不同** → 误走 `getCloudflareContext` 分支 → 仍连远程 | 改**运行时检测**：`navigator.userAgent === 'Cloudflare-Workers'` |

**关键教训**：只修 D-02 无效（第二次构建仍红），因为 worker 根本没走 `getPlatformProxy`。
判据必须选在**所有进程形态下都稳定**的信号 —— argv / NEXT_PHASE 都会漏，运行时环境不会。

**INV-BUILD-003（易踩坑）**：`remoteBindings` 不能一律设 false。
`deploy:database` 依赖 `payload migrate` 在 NODE_ENV=production 下打**远程** D1；
若一并降级为本地，会产生「迁移写进本地 SQLite、线上库未更新」的**静默故障**。

**验证**：`tsc --noEmit` 0 error；`next build --webpack` ✅ Compiled successfully、静态页 6/6；
Contract CI 仍 GREEN。产出 `docs/312-BUILD-BASELINE-AND-BINDING-SEMANTICS-v1.0.md`（含 ADR-B001）。

### 环境注意事项（本机）

- **bash 的 PATH 会被 shim 打断**（`ls`/`head`/`grep` 变 command not found）。
  每个 Bash 命令前加 `export PATH="/usr/bin:/bin:/usr/local/bin:$PATH"` 即可恢复。
- **无 pnpm**（未全局安装，corepack 也坏了）。改用 `node_modules/.bin/*` 直接跑，
  配 managed node：`C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`。
  next/tsc/payload/vitest/playwright 均在 `node_modules/.bin` 下可用。
- `next build` 约 1.5–3.5 分钟，务必 `run_in_background`。

### 约定（后续沿用）

- 新文档用 `300+` 编号段；Addendum 用 `NNNA`；编号永不复用。
- **不重命名/不移动**现有 225 份文档（交叉引用多），改用别名解析表消解编号冲突。
- 引用文档一律写**完整文件名**，编号仅作排序。
